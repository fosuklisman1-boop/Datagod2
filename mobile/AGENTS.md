# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v55.0.0/ before writing any code.

This project runs Expo SDK 55 (downgraded from 56 on 2026-06-11) so it loads in the
Expo Go app from the App Store / Play Store, which lags one SDK behind new releases.
Do not bump to SDK 56 until the store version of Expo Go supports it.
